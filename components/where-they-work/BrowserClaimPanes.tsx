'use client';

/**
 * Marketing re-export — the claim panes live in `@trooper/demo` so the live
 * browser workspace and the capability scene share one implementation.
 */
export {
  CLAIM_TABS,
  BrowserClaimPage,
  TabFavicon,
  type ClaimTabId,
} from '@trooper/demo';
