'use client';

/**
 * Marketing re-export — the editor mock lives in `@trooper/demo` so the
 * capability row and the live demo video workspace stay one implementation.
 */
export { VideoEditorCapabilityVisual as default } from '@trooper/demo';
